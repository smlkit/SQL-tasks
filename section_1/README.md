# SQL Practice — Online School Database

Практический набор SQL-задач для отработки:

- `INNER JOIN`
- `LEFT JOIN`
- `RIGHT JOIN`
- `FULL JOIN`
- `CROSS JOIN`
- `ANTI JOIN`
- `SELF JOIN`
- агрегатных функций
- `GROUP BY`
- `HAVING`
- `CASE WHEN`
- `CASE` внутри агрегатных функций
- `WHERE EXISTS`
- `WHERE NOT EXISTS`
- вложенных запросов
- `CTE`
- `VIEW`
- `CTAS`
- оконных функций

---

## Описание

В этом проекте используется учебная база данных онлайн-школы.

Основные сущности:

- студенты;
- преподаватели;
- курсы;
- группы;
- записи студентов на курсы;
- занятия;
- оценки;
- платежи.

Схема специально содержит связи, позволяющие практиковать как простые `JOIN`, так и более сложные комбинации нескольких SQL-концепций.

---

## Уровни сложности

| Уровень | Темы |
|---|---|
| 🟢 Beginner | JOIN, GROUP BY, базовые агрегаты |
| 🟡 Intermediate | CASE WHEN, HAVING |
| 🟠 Advanced | EXISTS, NOT EXISTS, подзапросы |
| 🔵 Expert | CTE, VIEW, CTAS, оконные функции |
| 🔴 Challenge | Комбинация нескольких тем |

---

# 🟢 Уровень 1 — JOIN

## 1. Список студентов и их курсов

Вывести список всех студентов, которые записались хотя бы на один курс.

Показать:

- имя и фамилию студента;
- название группы;
- дату записи;
- статус записи.

Использовать `INNER JOIN`.

---

## 2. Информация о записях на курсы

Получить список всех записей студентов на курсы.

Показать:

- студента;
- группу;
- курс;
- преподавателя;
- уровень курса;
- цену курса.

Использовать несколько `INNER JOIN`.

---

## 3. Все студенты и количество курсов

Вывести всех студентов и количество курсов, на которые они записались.

Студенты без записей также должны присутствовать в результате.

Использовать `LEFT JOIN`.

---

## 4. Все студенты и completed-курсы

Вывести всех студентов и количество их завершённых (`completed`) курсов.

Студенты без завершённых курсов также должны присутствовать в результате.

Использовать `LEFT JOIN`.

---

## 5. Все курсы и количество групп

Получить список всех курсов и количество групп, созданных для каждого курса.

Использовать `RIGHT JOIN`.

Курсы без групп также должны отображаться.

---

## 6. Преподаватели и их группы

Для каждого преподавателя показать:

- имя преподавателя;
- количество групп;
- количество студентов в его группах.

Преподаватели без групп также должны присутствовать в результате.

Использовать `LEFT JOIN`.

---

## 7. Студенты без курсов

Найти студентов, которые никогда не записывались ни на один курс.

Решить задачу через `LEFT JOIN` и проверку `NULL`.

Это задача на `ANTI JOIN`.

---

## 8. Курсы без групп

Найти курсы, для которых нет ни одной группы.

Использовать `LEFT JOIN`.

---

## 9. Преподаватель и руководитель

Вывести каждого преподавателя и его руководителя.

Результат должен содержать:

- преподавателя;
- руководителя.

Если у преподавателя нет руководителя, он всё равно должен присутствовать в результате.

Использовать `SELF JOIN`.

---

## 10. Преподаватели с высокой зарплатой

Найти преподавателей, которые получают больше своего руководителя.

Показать:

- имя преподавателя;
- зарплату преподавателя;
- имя руководителя;
- зарплату руководителя.

Использовать `SELF JOIN`.

---

## 11. FULL JOIN — студенты и платежи

Получить полный список студентов и платежей.

Показать:

- имя студента;
- `student_id`;
- `payment_id`;
- сумму платежа;
- статус платежа.

Необходимо сохранить:

- студентов, у которых нет платежей;
- платежи, которые невозможно сопоставить со студентом.

Использовать `FULL JOIN`.

Цель задачи — понять отличие `FULL JOIN` от `LEFT JOIN` и `RIGHT JOIN`.

---

## 12. FULL JOIN — курсы и группы

Получить полный список курсов и групп.

Показать:

- название курса;
- название группы;
- преподавателя;
- дату начала группы.

В результате должны присутствовать:

- курсы без групп;
- группы, для которых отсутствует соответствующий курс.

Использовать `FULL JOIN`.

Дополнительно создать колонку `record_type` со значениями:

- `Course and Group`
- `Course without Group`
- `Group without Course`

Для определения значения использовать `CASE WHEN`.

---

## 13. CROSS JOIN — расписание занятий

Предположим, что онлайн-школа хочет сформировать потенциальное расписание.

Создать все возможные комбинации:

- преподавателей;
- дней недели.

Использовать `CROSS JOIN`.

Результат должен содержать:

- `teacher_id`;
- `teacher_name`;
- `day_of_week`.

Дни недели задать самостоятельно:

- Monday
- Tuesday
- Wednesday
- Thursday
- Friday

Количество строк должно быть равно:

```text
количество преподавателей × 5
```

---

## 14. CROSS JOIN — варианты тарифов

Создать CTE с тарифами:

- `Basic`;
- `Standard`;
- `Premium`.

И CTE с длительностью обучения:

- `1 month`;
- `3 months`;
- `6 months`;
- `12 months`.

Получить все возможные комбинации тарифа и длительности.

Результат должен содержать:

- тариф;
- длительность;
- стоимость.

Использовать `CROSS JOIN`.

---

## 15. CROSS JOIN — сравнение студентов

Сформировать все возможные пары студентов.

Для каждой пары вывести:

- имя первого студента;
- имя второго студента;
- средний балл первого студента;
- средний балл второго студента;
- разницу в среднем балле.

Необходимо:

- исключить сравнение студента с самим собой;
- не выводить обе стороны одной пары.

Например:

```text
Anna — Max
```

и

```text
Max — Anna
```

должны считаться одной парой.

Для этого использовать условие:

```sql
student_1.student_id < student_2.student_id
```

Для расчёта среднего балла можно предварительно использовать CTE.

Использовать `CROSS JOIN`.

---

## 16. CROSS JOIN — потенциальные пары курсов

Сформировать все возможные пары курсов.

Для каждой пары вывести:

- название первого курса;
- название второго курса;
- категорию первого курса;
- категорию второго курса;
- разницу в цене.

Необходимо исключить:

- сравнение курса с самим собой;
- повторяющиеся пары.

Использовать `CROSS JOIN`.

Дополнительно оставить только пары курсов из разных категорий.

---

## 17. SELF JOIN — сравнение преподавателей

Найти пары преподавателей, работающих в одном департаменте.

Для каждой пары вывести:

- имя первого преподавателя;
- имя второго преподавателя;
- департамент;
- зарплату первого преподавателя;
- зарплату второго преподавателя.

Необходимо исключить:

- сравнение преподавателя с самим собой;
- дублирование пар.

Использовать `SELF JOIN`.

---

## 18. SELF JOIN — преподаватели с большей зарплатой

Для каждого преподавателя найти других преподавателей того же департамента, которые получают больше.

Показать:

- имя преподавателя;
- его зарплату;
- имя коллеги;
- зарплату коллеги;
- разницу в зарплате.

Использовать `SELF JOIN`.

---

## 19. SELF JOIN — студенты из одного города

Найти пары студентов, которые живут в одном городе.

Показать:

- имя первого студента;
- имя второго студента;
- город.

Не выводить:

- пару студента с самим собой;
- зеркальные дубликаты.

Использовать `SELF JOIN`.

---

## 20. SELF JOIN — курсы одной категории

Найти пары курсов, принадлежащих одной категории.

Показать:

- первый курс;
- второй курс;
- категорию;
- цену первого курса;
- цену второго курса;
- разницу в цене.

Использовать `SELF JOIN`.

---

# 🟡 Уровень 2 — Агрегатные функции

## 21. Статистика по курсам

Для каждого курса посчитать:

- количество групп;
- количество студентов;
- средний итоговый балл;
- максимальный итоговый балл;
- минимальный итоговый балл.

Использовать агрегатные функции и `GROUP BY`.

---

## 22. Статистика по городам

Для каждого города студентов посчитать:

- количество студентов;
- количество записей на курсы;
- средний итоговый балл.

---

## 23. Популярные курсы

Найти курсы, на которые записалось более 3 студентов.

Использовать `GROUP BY` и `HAVING`.

---

## 24. Сильные группы

Найти группы, в которых средний итоговый балл выше `85`.

Вывести:

- название группы;
- средний балл.

---

## 25. Статистика по преподавателям

Для каждого преподавателя посчитать:

- количество студентов;
- средний балл;
- максимальный балл;
- минимальный балл.

Преподаватели без студентов также должны присутствовать.

---

## 26. Статистика платежей

Для каждого студента посчитать:

- общую сумму оплаченных платежей;
- количество оплаченных платежей;
- средний оплаченный платёж.

Учитывать только платежи со статусом `paid`.

---

## 27. Самый дорогой курс

Найти курс с максимальной ценой.

Использовать агрегатную функцию `MAX()` и вложенный запрос.

---

# 🟡 Уровень 3 — CASE WHEN

## 28. Категория цены курса

Для каждого курса создать категорию цены:

| Цена | Категория |
|---|---|
| `< 30000` | Дешёвый |
| `30000–49999` | Средний |
| `50000–69999` | Дорогой |
| `>= 70000` | Премиум |

Использовать `CASE WHEN`.

---

## 29. Результат обучения

Для каждого студента определить результат обучения на основании `final_score`:

| Балл | Результат |
|---|---|
| `>= 90` | Отлично |
| `80–89` | Хорошо |
| `60–79` | Удовлетворительно |
| `< 60` | Не сдал |

Использовать `CASE WHEN`.

---

## 30. Успешные и неуспешные студенты

Для каждого курса посчитать:

- общее количество студентов;
- количество студентов с результатом `>= 90`;
- количество студентов с результатом `< 60`.

Использовать `CASE` внутри агрегатной функции.

Например:

```sql
SUM(
    CASE
        WHEN final_score >= 90
        THEN 1
        ELSE 0
    END
)
```

---

## 31. Платежи по способу оплаты

Для каждого курса посчитать:

- сумму всех оплаченных платежей;
- сумму платежей картой;
- сумму платежей наличными.

Использовать `CASE` внутри `SUM()`.

---

## 32. Статус студента

Для каждого студента определить статус:

- `VIP` — минимум 2 завершённых курса и минимум 2 оплаченных платежа;
- `Активный` — есть хотя бы один завершённый курс;
- `Новый` — есть только активная запись;
- `Неактивный` — нет записей на курсы.

Использовать `CASE WHEN`.

Для определения статуса потребуется объединить несколько источников данных и использовать агрегаты.

---

## 33. Классификация групп

Для каждой группы определить её уровень:

| Средний балл | Уровень |
|---|---|
| `>= 90` | Сильная |
| `75–89` | Средняя |
| `< 75` | Слабая |

Использовать `CASE WHEN` и агрегатную функцию.

---

## 34. Распределение студентов по результату

Для каждого преподавателя посчитать:

- количество студентов с оценкой `>= 90`;
- количество студентов с оценкой `70–89`;
- количество студентов с оценкой `< 70`.

Получить все три значения в одной строке.

Использовать `CASE` внутри агрегатных функций.

---

# 🟠 Уровень 4 — EXISTS / NOT EXISTS

## 35. Студенты с завершёнными курсами

Найти студентов, у которых существует хотя бы один завершённый (`completed`) курс.

Использовать:

```sql
WHERE EXISTS (...)
```

---

## 36. Крупные платежи

Найти студентов, у которых существует хотя бы один платёж на сумму больше `50000`.

Использовать `WHERE EXISTS`.

---

## 37. Студенты Data-направления

Найти студентов, которые записались хотя бы на один курс категории `Data`.

Использовать `WHERE EXISTS`.

---

## 38. Студенты без оплаченных платежей

Найти студентов, у которых нет ни одного платежа со статусом `paid`.

Использовать `WHERE NOT EXISTS`.

---

## 39. Курсы без групп

Найти курсы, для которых не существует ни одной группы.

Решить задачу через `WHERE NOT EXISTS`.

Не использовать `LEFT JOIN`.

---

## 40. Завершённые, но не отменённые

Найти студентов, которые:

- имеют хотя бы один `completed`-курс;
- не имеют ни одной `cancelled` записи.

Использовать одновременно:

- `EXISTS`;
- `NOT EXISTS`.

---

# 🟠 Уровень 5 — Вложенные запросы

## 41. Курсы выше средней цены

Найти курсы, цена которых выше средней цены всех курсов.

Использовать вложенный запрос с `AVG()`.

---

## 42. Студенты выше среднего

Найти студентов, чей средний итоговый балл выше среднего балла всех студентов.

---

## 43. Студенты определённого преподавателя

Найти всех студентов, которые когда-либо учились у преподавателя `Екатерина Павлова`.

Использовать вложенный запрос с `IN`.

---

## 44. Количество студентов через коррелированный подзапрос

Для каждого курса вывести:

- название курса;
- цену курса;
- количество студентов.

Количество студентов получить с помощью коррелированного подзапроса.

---

# 🔵 Уровень 6 — CTE / VIEW / CTAS

## 45. CTE — статистика студентов

Создать CTE, который рассчитывает для каждого студента:

- количество завершённых курсов;
- сумму оплаченных платежей;
- средний итоговый балл.

Во внешнем запросе оставить только студентов, которые:

- имеют минимум 2 завершённых курса;
- заплатили больше `50000`.

---

## 46. VIEW — статистика студентов и курсов

Создать представление:

```text
student_course_stats
```

Представление должно содержать:

- студента;
- курс;
- группу;
- преподавателя;
- итоговый балл;
- цену курса;
- статус записи.

После создания `VIEW` написать запрос, который показывает средний балл по каждому курсу.

---

## 47. CTAS — статистика курсов

Создать новую таблицу:

```text
course_performance
```

используя `CREATE TABLE AS SELECT`.

В таблице сохранить для каждого курса:

- название;
- количество студентов;
- средний балл;
- максимальный балл;
- минимальный балл;
- количество студентов с баллом `>= 80`.

После создания таблицы вывести курсы со средним баллом выше `85`.

---

# 🔵 Уровень 7 — Оконные функции

## 48. ROW_NUMBER — история обучения

Для каждого студента пронумеровать его записи на курсы от самой новой к самой старой.

Получить:

- студента;
- курс;
- дату записи;
- порядковый номер записи.

Использовать:

```sql
ROW_NUMBER() OVER (
    PARTITION BY student_id
    ORDER BY enrollment_date DESC
)
```

---

## 49. RANK — рейтинг студентов

Составить рейтинг студентов по их среднему итоговому баллу.

Вывести:

- студента;
- средний балл;
- место в рейтинге.

Использовать `RANK()`.

---

## 50. RANK — рейтинг внутри курса

Для каждого курса составить рейтинг студентов по `final_score`.

Результат должен содержать:

- курс;
- студента;
- итоговый балл;
- место.

Использовать:

```sql
RANK() OVER (
    PARTITION BY course_id
    ORDER BY final_score DESC
)
```

После этого оставить только студентов, занявших первое место в каждом курсе.

---

# 🔴 Challenge — комбинированные задачи

## 51. Рейтинг курсов

Для каждого курса вывести:

- название;
- категорию;
- цену;
- количество студентов;
- средний балл;
- количество студентов с баллом `>= 90`;
- процент студентов с баллом `>= 80`;
- количество завершённых записей;
- количество отменённых записей;
- общую сумму оплаченных платежей;
- место курса внутри категории по выручке.

Требования:

- использовать `LEFT JOIN`;
- использовать `GROUP BY`;
- использовать `CASE WHEN`;
- использовать `CASE` внутри агрегатных функций;
- использовать оконную функцию `RANK()`.

Курсы без студентов не должны исчезнуть из результата.

---

## 52. Лучшие студенты

Найти студентов, которые одновременно:

- имеют минимум 2 завершённых курса;
- имеют средний балл выше среднего балла всех студентов;
- имеют хотя бы один оплаченный платёж;
- входят в топ-3 студентов своего города по среднему баллу.

При решении использовать комбинацию:

- `HAVING`;
- `EXISTS`;
- вложенного запроса;
- оконной функции.

---

## 53. Анализ преподавателей

Для каждого преподавателя вывести:

- зарплату;
- количество групп;
- количество студентов;
- средний балл студентов;
- зарплату руководителя;
- разницу между зарплатой преподавателя и руководителя;
- место преподавателя по среднему баллу внутри своего `department`.

Использовать:

- `SELF JOIN`;
- `LEFT JOIN`;
- агрегатные функции;
- оконную функцию.

---

## 54. Финальная задача — Course Performance Report

Сформировать итоговый аналитический отчёт по курсам.

Для каждого курса вывести:

- название курса;
- категорию;
- уровень;
- цену;
- количество студентов;
- средний балл;
- процент студентов с баллом `>= 80`;
- количество завершивших курс;
- количество отменённых записей;
- общую сумму оплаченных платежей;
- место курса внутри категории по выручке.

### Дополнительные требования

1. Курсы без студентов не должны исчезнуть из результата.
2. Для подсчёта успешных студентов использовать `CASE` внутри агрегатной функции.
3. Для расчёта выручки использовать `CASE` внутри `SUM()`.
4. Для рейтинга использовать `RANK()`.
5. Рейтинг должен рассчитываться отдельно внутри каждой категории.
6. Постарайтесь решить задачу так, чтобы не использовать коррелированные подзапросы там, где можно обойтись `JOIN` + `GROUP BY`.
